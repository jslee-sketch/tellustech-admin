---
title: "Tellustech ERP — 고객 포탈 가이드"
subtitle: "고객사 담당자용 (v2 — 포탈 확장 반영)"
author: "Tellustech IT 팀"
date: "2026-04-28"
lang: ko
---

# 환영합니다

Tellustech 의 IT 렌탈·계측기 렌탈·정비 서비스를 이용해 주셔서 감사합니다.

**고객 포탈**은 단순한 AS 요청 창구를 넘어, OA·계측기 두 사업부의 모든 서비스를 한 곳에서 관리하는 통합 플랫폼입니다.

## 무엇이 가능한가요?

### OA 사업부문 (프린터·복합기)
- 📠 **OA 렌탈 현황** — 내 계약·장비·결제 현황 한눈에
- 🛠 **AS 요청** — 본인 보유 장비 고장 신고
- 📦 **소모품 요청** — 토너·잉크·부품 등 호환 품목 주문
- ✍️ **사용량 컨펌** — 월별 청구 사용량 확인 후 모바일 서명

### 계측기 사업부문
- 🔬 **렌탈 / 수리 / 교정 / 유지보수 / 매매 현황** — 5종 서비스별 결제까지
- 📄 **교정 성적서 다운로드**

### 소통하기 (NEW)
- 💬 **견적 요청** — IT 렌탈·계측기·교정·수리·매매 견적 즉시 요청
- 🤝 **업체 추천** — 거래처 추천 시 첫 입금 발생하면 100,000d 적립
- 🌟 **칭찬 / 개선 / 제안** — 직원 칭찬·서비스 개선 의견
- 📰 **소식** — 마케팅·공휴일·산업 동향·사용 팁
- 📊 **서베이** — 고객 만족도 조사 참여 시 10,000d

### 🏆 포인트 시스템 (NEW)
모든 활동이 자동 적립됩니다. AS 요청·소모품 요청·서비스 확정·사용량 컨펌마다 1,000d, 견적 요청·의견 등록 1,000d, 게시글 보너스 가변, 서베이 10,000d, 추천 계약 첫 입금 100,000d. 1,000,000d 부터 청구액 차감 또는 상품권으로 교환 가능.

PC·태블릿·스마트폰 어느 화면에서나 동일하게 작동하며, 한국어 / 베트남어 / 영어 3개 언어를 지원합니다.

> 본 가이드는 **고객사 담당자**가 포탈을 사용할 때 참고합니다. 사내 직원용 ERP 매뉴얼(별책 A)과는 다른 별도 문서입니다.

---

# 1. 시작하기 — 접속과 로그인

## 1.1 포탈 접속

브라우저(Chrome / Safari / Edge)에서 Tellustech 담당자가 안내한 포탈 주소로 접속합니다.

```
https://portal.tellustech.co.kr
```

> 정확한 도메인은 담당자에게 확인하세요. 사내 직원용 도메인과 분리되어 있습니다.

## 1.2 로그인 입력값 — 어디로 흘러가는가

| 입력 항목 | 예시 | **이 값이 시스템에서 하는 일** |
|---|---|---|
| **고객코드** | `CL-260101-001` | DB 의 `Client.clientCode` 와 매칭 → 본인 거래처 ID 가 세션에 고정. 이후 모든 화면이 이 ID 로 데이터 필터 |
| **비밀번호** | (담당자 발급) | bcrypt 해시 비교. 일치 시 JWT 발급, 불일치 5회면 일시 잠금 |
| **언어** | 한국어 / Tiếng Việt / English | `User.language` 에 저장 → 모든 화면 라벨·메시지에 즉시 적용. 데이터 자체는 3언어 컬럼에 함께 저장됨 |

**입력 결과:**
- 로그인 성공 → `JWT` 쿠키(`tts_session`) 발급, `/portal` 메인으로 이동.
- 로그인 후 표시되는 모든 데이터(IT 계약·요청 현황 등)는 자동으로 본인 거래처 ID 로 필터링됩니다. 다른 회사 데이터는 절대 노출되지 않습니다.
- 비밀번호 5회 오답 시 계정 일시 잠금 (담당자 해제 필요).

> **회사코드(TV/VR) 입력은 필요 없습니다.** 귀사가 거래하는 Tellustech 법인이 TV 인지 VR 인지는 고객코드 자체에 이미 연결되어 있어 자동 라우팅됩니다.

## 1.3 로그인 화면 진입

로그인 페이지에서 화면 하단의 **🛒 고객 포탈 로그인 / Customer Portal** 버튼을 누르거나, 안내된 URL 에 `?portal=1` 파라미터가 붙어 있으면 자동으로 포탈 모드 폼이 표시됩니다 (회사코드 입력란이 사라지고 「고객코드」로 변경).

## 1.4 비밀번호 분실

비밀번호를 잊으신 경우 Tellustech 담당자에게 연락해 재설정을 요청해 주세요. 보안상 자가 재설정은 제공하지 않습니다 (포탈에서는 이메일 기반 재설정 링크를 발송하지 않습니다 — 사칭 방지 목적).

---

# 1.4.5 회원 정책 (NEW)

## 한 거래처 = 한 개의 포탈 ID

귀사(거래처)에 발급되는 포탈 로그인 ID는 **한 개**입니다. 한 회사 내 여러 담당자가 함께 사용할 경우, 비밀번호와 ID는 IT 담당자 한 명이 관리하시고 필요 시 공유해 주세요.

> **왜 한 개만 발급하나요?**
> AS·소모품 요청·견적 요청·서베이 등 모든 활동이 한 거래처 단위로 누적됩니다. 여러 ID를 발급하면 같은 활동에 중복 적립되거나, 분산되어 책임 추적이 어려워집니다. 또한 포인트는 거래처(회사) 자산으로 처리되어 청구액 차감 등 회사 단위 사용으로 통합됩니다.

## 자가 회원가입 없음

포탈은 **관리자 발급 방식** 입니다. 신규 거래처 등록 시 Tellustech 가 ID를 발급하여 IT 담당자에게 전달합니다. 비밀번호 분실 시 자가 재설정은 제공하지 않으며, 담당자에게 연락하셔서 재발급 받으세요 (사칭 방지 목적).

## 포인트는 거래처 자산

한 명의 직원이 견적을 요청해 +1,000d 가 적립되든, 다른 직원이 서베이에 응답해 +10,000d 가 적립되든, **모두 거래처 잔액 한 곳으로 누적**됩니다. 사용 시에도 회사 단위로 사용됩니다 (개인이 가져갈 수 없음).

## 포인트 사용 정책 — 계약 시 결정 (NEW)

대기업 컴플라이언스 (개인 상품권 수령 금지 등) 대응을 위해, **포인트 사용 방식은 계약 시점에 결정**됩니다.

| 정책 | 의미 |
|---|---|
| **❌ 미설정 (NONE)** | 적립만 가능, 교환 불가 — 정책 결정까지 대기 |
| **💰 청구액 차감만** | 다음 IT 계약/매출 청구액에서 자동 차감만 가능 |
| **🎫 상품권만** | 기프티콘 수령만 가능 |
| **✅ 둘 다** | 신청 시 직접 선택 가능 |

귀사의 정책은 사이드바 🏆 클릭 → 포인트 페이지에서 안내문으로 표시됩니다 ("계약상 사용 방식: 청구액 차감만"). 정책에 맞지 않는 옵션은 **자동으로 숨김** 처리됩니다.

> 정책 변경이 필요하시면 영업담당에게 문의해 주세요.

---

# 1.5 사이드바 (NEW — Phase A 도입)

로그인 후 좌측에 **3그룹 사이드바**가 표시됩니다 (PC) 또는 상단 햄버거 ☰ 메뉴 (모바일).

## 1.5.1 PC 사이드바 (가로 ≥ 768px)

```
┌─────────────────────────┐
│ TELLUSTECH PORTAL    ◀  │ ← 접기 토글
│ 회사명                   │
│ 🏆 87,000d              │ ← 포인트 잔액 (클릭 → /portal/points)
│                          │
│ ── OA 사업부문 ──    ▼  │ ← 섹션 접기/펼치기
│ 📠 [OA 광고 배너]        │
│   렌탈 현황              │
│   AS 요청                │
│   소모품 요청            │
│   사용량 컨펌            │
│                          │
│ ── 계측기 사업부문 ── ▼ │
│ 🔬 [TM 광고 배너]        │
│   렌탈 / 수리 / 교정 ... │
│                          │
│ ── 소통하기 ──       ▼  │
│ 💬 견적 요청             │
│ 🤝 업체 추천             │
│ 🌟 칭찬·개선·제안        │
│ 📰 소식                  │
│ 📊 서베이                │
│                          │
│ 🇻🇳 🇺🇸 🇰🇷  ☀/🌙     │ ← 언어/테마
│ [로그아웃]              │
│ [🏢 사내 직원 로그인]   │
└─────────────────────────┘
```

- **◀** 접기 버튼 → 64px 아이콘만 표시 (다시 ▶ 클릭으로 펼침)
- **섹션 헤더 ▼/▶** 클릭으로 그룹별 펼침·접힘. 새로고침 후에도 상태 유지(localStorage).
- 현재 페이지의 그룹은 자동으로 펼쳐집니다.
- **🏢 사내 직원 로그인** — 같은 시스템의 사내 ERP 로 전환할 때 (자동 로그아웃 후 사내 로그인 화면 이동).

## 1.5.2 모바일 햄버거 메뉴 (가로 < 768px)

스마트폰에서는 사이드바가 자동으로 숨겨지고 상단에 햄버거 메뉴가 나타납니다.

```
┌──────────────────────────┐
│ ☰  TELLUSTECH  🏆 87,000d │
└──────────────────────────┘
```

- **☰** 클릭 → 좌측에서 슬라이드 인 (slide-in) 으로 280px drawer 등장.
- 메뉴 항목 클릭 시 자동으로 drawer 닫힘.
- 회전 (가로/세로) 자유 — manifest 가 `orientation: any` 로 설정됨.

## 1.5.3 광고 배너 (사업부문 헤더 아래)

`📠 OA 광고 / 🔬 TM 광고` 텍스트는 Tellustech 관리자가 변경 가능한 한 줄 광고입니다. 클릭 시 새 탭으로 회사 홈페이지 (예: tellustech.co.kr/oa) 가 열립니다.

---

# 2. 메인 화면 한눈에 보기

로그인 직후 표시되는 **메인 화면**은 4개 영역으로 구성됩니다.

## 2.1 헤더 — 회사명 / 고객코드

화면 상단에 귀사의 회사명과 고객코드(`CL-...`)가 표시됩니다.
**미수금 정지 상태**(상환 지연 등)인 경우 빨간색 `정지 / Bị khóa` 배지가 표시되며, AS·소모품 신규 요청이 일시적으로 비활성화됩니다. 해제는 결제 확인 후 자동 처리되거나 담당자에게 문의해 주세요.

## 2.2 빠른 요청 카드 — 4개

- 🛠 **AS 요청 / Yêu cầu BH**
- 📦 **소모품 요청 / Yêu cầu vật tư**
- ✍️ **사용량 컨펌 / Xác nhận sử dụng**
- 📄 **교정 성적서 / Chứng chỉ** (괄호 안 숫자는 다운로드 가능 건수)

각 항목을 누르면 해당 화면으로 이동합니다.

## 2.3 내 IT 계약 카드

활성/임박 IT 계약 최근 10건을 계약번호 순으로 표시합니다. 각 줄에는 **계약번호**, **상태**(`ACTIVE` / `DRAFT` 등), **장비 수**가 함께 보입니다.

## 2.4 내 요청 현황 표

직전에 등록한 AS·소모품 요청 최근 20건의 진행 상황입니다. 4단계로 흐릅니다.

| 단계 | DB 값 | 의미 | 누가 바꾸는가 |
|---|---|---|---|
| **요청 / Yêu cầu** | `RECEIVED` | 접수 완료, 담당자 배정 대기 | 자동 (등록 시점) |
| **진행중 / Đang xử lý** | `IN_PROGRESS` | 담당자 배정·작업 진행 중 | Tellustech 담당자가 변경 |
| **완료 / Hoàn tất** | `COMPLETED` | 작업 완료, 고객 확인 대기 | Tellustech 담당자가 디스패치 완료 + 서명 |
| **확인됨 / Đã xác nhận** | `CONFIRMED` | 고객이 확인 → 종결 | **귀사**가 「확인」 버튼 클릭 |

### 「확인」 버튼이 하는 일

상태가 `COMPLETED` 인 줄에만 우측에 **확인** 버튼이 활성화됩니다. 이 버튼을 누르면:

1. API `POST /api/portal/tickets/{id}/confirm` 호출.
2. 서버 검증: 본인 거래처의 티켓인가? 현재 상태가 `COMPLETED` 인가?
3. 통과 시 → `status = CONFIRMED`, `confirmedAt = 지금` 으로 업데이트 → 작업 종결.
4. 페이지가 자동 새로고침되어 줄이 「확인됨」 으로 갱신.

> 한 번 확인하면 되돌릴 수 없습니다. 작업 결과에 이의가 있으면 **확인 버튼을 누르기 전에** Tellustech 담당자에게 연락해 주세요.

---

# 3. AS 요청

장비가 고장났거나 점검이 필요할 때 사용합니다.

## 3.1 입력값별 데이터 영향도

| 입력 항목 | 출처 / 값 | **이 값이 시스템에서 하는 일** |
|---|---|---|
| **장비 SN** (드롭다운) | `/api/portal/my-equipment` 가 반환하는 본인 활성 장비 목록 | 선택한 SN 이 `AsTicket.serialNumber` 에 저장 → Tellustech 담당자가 그 장비의 계약·이력·교정 기록을 즉시 조회 |
| **원문 언어** (VI/KO/EN) | 라디오 / 셀렉트 | `AsTicket.originalLang` 에 저장. 자동 번역 시 어느 언어를 원본으로 볼지 시스템이 판단하는 기준 |
| **증상 (VI/KO/EN)** 3칸 중 1칸 | 자유서술 텍스트 | 입력 즉시 저장 시 Claude API 가 나머지 2개 언어로 자동 번역 → `symptomVi` / `symptomEn` / `symptomKo` 3컬럼에 모두 채움. 결과: 한국어로 쓴 내용을 베트남 담당자가 베트남어로, 영국 본사가 영어로 즉시 읽음 |

**드롭다운에 표시되는 형식:**
```
SN-12345 · ITM-260101-001 LaserPrinter X7500 (TLS-260101-001)
```
순서대로 **시리얼 번호 · 품목코드 품목명 (계약번호)** 입니다.

**드롭다운에 어떤 SN 이 보이는가:**
- 활성 IT 계약(`itContractEquipment.removedAt = null`)에 등록된 장비 + 종료 전 TM 렌탈(`tmRentalItem.endDate >= 오늘`)의 장비.
- 따라서 회수·교체된 장비는 자동으로 사라지고, 신규 등록된 장비는 자동으로 보입니다 (페이지 새로고침 후).

## 3.2 입력값 검증 규칙 (저장 시점)

| 검증 | 실패하면 |
|---|---|
| 증상 3개 칸 중 **최소 1개**에 텍스트가 있어야 함 | `invalid_input` 에러, 「증상을 입력하세요」 표시 |
| 본인 거래처 세션 (역할=CLIENT) | `client_only` 에러 — 정상 흐름에서는 발생하지 않음 |
| 자유 입력 SN 도 허용 | 드롭다운에 없는 SN 도 그대로 저장됨 (외부에서 가져온 장비 등) |

## 3.3 접수 후 일어나는 일

**AS 접수 / Tiếp nhận BH** 버튼 클릭 시 시스템이 다음을 한 번에 수행합니다.

1. **티켓번호 자동 발급** — `YY/MM/DD-NN` 형식 (예: `26/04/27-01`) — 그 날의 일련번호.
2. **티켓 생성** — `kind = AS_REQUEST`, `status = RECEIVED`, 접수일시·본인 거래처 ID 자동 채움.
3. **3언어 번역 보관** — 입력하지 않은 두 언어 칸이 자동 번역되어 저장.
4. **사내 알림** — Tellustech AS 담당자에게 즉시 통지 (사내 ERP 「AS 접수」 화면에 빨간 미읽음 배지로 표시).
5. **고객 알림** — 등록된 이메일 또는 카카오톡으로 「접수 완료 — 번호 26/04/27-01」 메시지 전송.
6. **메인 화면 갱신** — 「내 요청 현황」에 즉시 새 줄로 표시.

## 3.4 미수금 정지(BLOCKED) 시 차단 동작

귀사 거래처가 `BLOCKED` 상태이면:
- 「AS 요청」 빠른 카드가 회색 + 취소선으로 표시.
- 강제로 URL 직접 입력 시 API 가 `403 receivable_blocked` 로 거절.
- 결제 확인되면 거래처 상태가 `WARNING` 또는 `NORMAL` 로 자동 복귀 → 메뉴도 즉시 활성화.

---

# 4. 소모품 요청

토너·잉크·부품 등 정기 소모품을 요청합니다.

## 4.1 입력값별 데이터 영향도

| 입력 항목 | 출처 / 값 | **이 값이 시스템에서 하는 일** |
|---|---|---|
| **품목** (드롭다운) | `/api/portal/my-supplies` — 본인 활성 장비의 호환 매핑(`ItemCompatibility`) 결과 | 선택한 itemId 가 라인에 저장. 호환 화이트리스트에 없는 itemId 는 서버가 `not_compatible` 로 거절 |
| **수량** | 1 이상 정수 | 라인의 `quantity` 에 저장. Tellustech 출고 담당자가 이 값으로 발주·창고 출고 처리 |
| **메모** (선택) | 자유 텍스트 | 라인의 `note` 에 저장. 색상·모델·긴급 여부 등을 담당자에게 직접 전달 |

## 4.2 호환 매핑이 어떻게 결정되는가

드롭다운에 보이는 품목은 다음 SQL 과 동등한 흐름으로 계산됩니다.

```
본인 활성 장비의 itemId 집합 (PRODUCT)
   ↓
ItemCompatibility 테이블의 (productItemId, consumableItemId) 매핑
   ↓
consumableItemId 집합 = 드롭다운 표시 후보
```

따라서:
- **신규 장비가 등록**되어도 호환 매핑이 같이 등록되지 않으면 새 소모품이 안 보일 수 있습니다 → 담당자에게 매핑 등록 요청.
- **장비가 회수**되면 그 장비 전용 소모품은 다른 장비와 공통이 아닐 경우 자동으로 드롭다운에서 사라집니다.
- 호환 매핑은 관리자 화면(`/admin/item-compatibility`)에서 Tellustech 가 직접 관리.

## 4.3 다중 라인 입력

`+ 품목 추가 / Thêm vật tư` 버튼으로 한 요청에 여러 품목을 함께 담을 수 있습니다. **모든 라인이 한 접수번호로 묶입니다** — 출고 담당자가 한 번의 출고 작업으로 처리합니다.

## 4.4 접수 후 일어나는 일

**요청 전송 / Gửi yêu cầu** 버튼 클릭 시:

1. **각 라인 호환성 검증** — 한 라인이라도 호환 외 품목이면 전체 거절(`not_compatible` + 해당 itemId 표시).
2. **접수번호 발급** — `YY/MM/DD-NN` 형식 (AS 와 동일 시퀀스 공유).
3. **티켓 생성** — `kind = SUPPLIES_REQUEST`, `status = RECEIVED`, 라인 정보가 `suppliesItems` JSON 컬럼에 저장.
4. **요약 메시지 자동 생성** — 「[소모품 요청] 토너 흑백 × 2, 잉크 컬러 × 1」 식으로 첫 화면에 노출.
5. **사내 알림** — Tellustech 출고 담당자에게 통지.
6. **메인 화면 갱신** — 「내 요청 현황」에 즉시 표시 (종류 컬럼에 「📦 소모품」 표기).

> 미수금 정지(`BLOCKED`) 시 AS 와 동일하게 빠른 카드가 비활성화됩니다.

---

# 5. 사용량 컨펌 (NEW — SNMP 자동수집 연동)

IT(프린터·복합기) 월별 사용량을 확인하고 서명·컨펌합니다.

## 새 워크플로 (2026-04 부터)

기존: 관리자가 수동으로 카운터 입력 → 청구 발행
**신규**: 에이전트가 매월 자동 수집 → 사용량 확인서(UC) 자동 생성 → 알림 → 고객 서명 → PDF → 매출

### 자동 수집 흐름
1. 매월 [수집일] (계약마다 다름, 기본 25일) 에 사무실 PC 에 설치된 에이전트가 SNMP 카운터 자동 수집
2. ERP가 「사용량 확인서 (UC-YYMMDD-###)」 자동 생성 (status=COLLECTED)
3. 다음날 03:00 KST cron 이 고객에게 알림 발송 (status=CUSTOMER_NOTIFIED)
4. 포탈 사이드바 알림 + 메인 화면에 「확인 대기」 표시
5. 고객 [확인하기] 클릭 → 장비별 사용량/요금 검토 → 서명 → 확정 (status=CUSTOMER_CONFIRMED)
6. 관리자 검토 → PDF 생성 → 매출 전표 발행 → 미수금 자동 발생

### 신규 화면 — 장비별 상세 카드

각 IT 계약의 장비별로 개별 카드가 표시됩니다:

```
┌─ 장비 1: Samsung SCX-8123 (SEC30CDA760C1FD) ─┐
│              │  흑백  │  컬러  │
│ 누적 사용량 │ 70,767 │   —    │
│ 전월 사용량 │ 69,500 │   —    │
│ 이번달 사용량│ 1,267 │   —    │
│ 기본료              500,000₫  │
│ 기본 포함 (흑백)   1,000매    │
│ 추가 사용 (흑백)     267매    │
│ 추가 과금 (@500₫)   133,500₫  │
│ ────────────────────────────  │
│ 소계                633,500₫  │
└────────────────────────────────┘

(장비 N대 → N개 카드)

═══════════════════════════════
합계: 1,433,500₫
[✍️ 서명]  [확인]  → +1,000d 적립!
```

### ⚠ 카운터 이상 감지

값이 불자연스럽게 줄었거나(메인보드 교체 등) 0보다 작은 사용량이 계산되면 ⚠ 표시. **확인 전에** Tellustech 담당자에게 연락해 주세요.

## 5.1 표시 데이터 — 어디서 오는가

화면에 보이는 각 줄은 사내 직원이 등록한 **월별 청구 행(`itMonthlyBilling`)** 1건입니다. 자동으로 본인 거래처의 IT 계약 청구만 노출됩니다.

| 컬럼 | 출처 / 의미 |
|---|---|
| **월 / Tháng** | `billingMonth` (예: `2026-04`) — Tellustech 담당자가 매월 등록 |
| **S/N** | 그 청구가 가리키는 IT 장비의 시리얼 |
| **흑백 / B&W** | `counterBw` — 사내 담당자가 검침해 입력한 누적값 |
| **컬러 / Color** | `counterColor` — 동일 |
| **금액 / Phí** | `computedAmount` — 시스템이 카운터 차이 × 단가로 자동 계산 |

## 5.2 입력값별 데이터 영향도

| 입력 / 동작 | **이 값이 시스템에서 하는 일** |
|---|---|
| **✍️ 서명** (canvas 그리기) | 손가락/마우스 궤적이 PNG (data URL) 로 변환되어 메모리에만 보관 — 아직 DB 저장 안 됨 |
| **확인 / Xác nhận** 버튼 | 서명 PNG 가 API 로 전송 → `itMonthlyBilling.customerSignature` 컬럼에 저장 → 그 청구 행이 잠김 (재서명 불가) → 사내 ERP 의 미수금 자동 발생 트리거 |

## 5.3 모바일 손가락 서명 절차

1. 청구 줄 우측의 **✍️ 서명 / Ký tên** 버튼을 누릅니다 → 서명 모달이 열림.
2. 모달 안의 흰색 영역에 마우스(또는 스마트폰 손가락)로 서명을 그립니다.
3. 모달의 **확인** 버튼으로 서명을 저장합니다. (모달 닫히고 줄에 ✓ 표시)
4. 표 줄의 **확인 / Xác nhận** 버튼을 누르면 서명이 서버로 전송되고 컨펌이 완료됩니다.

## 5.4 검증 규칙 (저장 시점)

| 검증 | 실패하면 |
|---|---|
| 본인 거래처의 청구 행만 컨펌 가능 | `forbidden` 에러 |
| 이미 서명된 행은 재서명 불가 | `already_confirmed` 에러 표시 |
| 서명 누락 시 「확인」 버튼 비활성화 | 클라이언트에서 차단 |

## 5.5 컨펌 후 일어나는 일

1. 청구 행이 잠김 (`customerSignature` not null).
2. 사내 ERP 미수금 모듈에서 그 청구액이 **새 미수금**(`PayableReceivable`, `kind=RECEIVABLE`, `status=OPEN`)으로 자동 생성.
3. 결제 만기일이 거래처 마스터의 결제 조건(예: 30일)으로 자동 계산.
4. 회계마감(잠금) 정책의 영향을 받기 시작.

## 5.6 정지(BLOCKED) 상태와 무관

사용량 컨펌은 미수금 정지 상태와 관계없이 항상 사용할 수 있습니다 — 컨펌 자체가 결제와는 별개의 「수치 확정」 절차이기 때문입니다.

> 한 번 컨펌하면 수정할 수 없습니다. 카운터 값에 이의가 있을 경우 **반드시 컨펌 전에** Tellustech 담당자에게 연락해 주세요.

---

# 6. 교정 성적서 다운로드

교정(검교정) 작업이 완료된 장비의 성적서 PDF 를 검색·다운로드합니다.

## 6.1 입력값별 데이터 영향도

각 검색 조건은 서버 쿼리에 부분 일치(`contains`) AND 조건으로 더해집니다. 모두 비워 두고 「조회」를 누르면 본인 거래처의 모든 성적서가 발행일 내림차순으로 표시됩니다.

| 조건 | 매칭 컬럼 | **검색 동작** |
|---|---|---|
| **S/N** | `SalesItem.serialNumber` | 부분 일치 (대소문자 무시) — 예: `SN-12` 입력 시 `SN-12345`, `ASN-12` 등 모두 매치 |
| **성적서번호** | `SalesItem.certNumber` | 부분 일치 |
| **품목** | `Item.itemCode` 또는 `Item.name` | 부분 일치 — 코드/이름 어느 쪽이든 |
| **시작일** | `SalesItem.issuedAt >= 시작일` | 발행일 하한 |
| **종료일** | `SalesItem.issuedAt <= 종료일` | 발행일 상한 |

## 6.2 어떤 행이 검색 대상인가 — 자동 필터

본 화면이 보여주는 데이터는 다음 4가지 조건이 동시에 만족되는 매출 라인입니다.

1. `Sales.clientId` = 본인 거래처 (자동 — 다른 거래처 데이터는 절대 안 보임)
2. `Sales.project.salesType` = `CALIBRATION` (교정 프로젝트만)
3. `SalesItem.certNumber` 또는 `SalesItem.certFileId` 중 **하나라도** 값이 있음
4. (위에 추가로) 화면 검색 조건이 매치

따라서 일반 매출(`TRADE` 등) 라인이나 PDF 가 아직 업로드 안 된 교정 라인은 노출되지 않습니다.

## 6.3 결과 표

| 컬럼 | 의미 |
|---|---|
| **발행일** | `SalesItem.issuedAt` |
| **성적서번호** | `SalesItem.certNumber` |
| **S/N** | `SalesItem.serialNumber` |
| **품목** | `itemCode · name` |
| **다음 기한** | `SalesItem.nextDueAt` — 다음 정기 검교정 권장일 |
| **다운로드** | 📄 PDF 버튼 — `certFileId` 가 있을 때만 표시 |

## 6.4 다운로드 동작

📄 PDF 버튼을 누르면 새 창에서 `/api/files/{certFileId}` 가 열립니다.
- 본인 거래처의 파일만 다운로드 가능 — 다른 거래처 ID 의 파일을 직접 URL 로 시도해도 `403` 거절.
- 파일이 열리면 브라우저의 인쇄·다운로드 메뉴로 로컬 저장 가능.
- 메인 화면 「📄 교정 성적서 ({N})」 의 N 값은 본 화면이 노출 가능한 총 건수와 같습니다.

---

# 7. 서비스 현황판 6종 (NEW)

OA·계측기 모든 서비스의 **계약 + 결제 현황**을 한 화면에서 확인합니다.

## 7.1 6종 서비스

| 메뉴 | 경로 | 데이터 |
|---|---|---|
| 📠 OA 렌탈 현황 | `/portal/oa/rentals` | IT 계약 + 장비 + 월별 청구 |
| 🔬 TM 렌탈 현황 | `/portal/tm/rentals` | TM 렌탈 + 장비 라인 + 매출 |
| 🔧 수리 현황 | `/portal/tm/repairs` | 수리 매출 건별 |
| 📐 교정 현황 | `/portal/tm/calibrations` | 교정 매출 + 성적서 |
| 🛠️ 유지보수 현황 | `/portal/tm/maintenance` | 유지보수 계약 + 월 안분 |
| 💼 매매 현황 | `/portal/tm/purchases` | 신품·중고 구매 이력 |

## 7.2 화면 구성 — 공통

각 현황판은 동일한 패턴입니다.

### (있을 때) 내 계약 / 렌탈 — 카드형 펼침
```
TLS-260426-007  🟢ACTIVE  5대  2026-01-01 ~ 2027-12-31  ▶
```
▶ 클릭 → 장비 라인 펼침 (S/N · 품목명 · 월정액 · 설치일).

### 결제 현황 — 표

| 컬럼 | 의미 |
|---|---|
| **월 / 건** | 청구 월 또는 전표번호 |
| **청구액** | `PayableReceivable.amount` |
| **예정일** | `revisedDueDate ?? dueDate` (변경일 우선) |
| **실제지급일** | `status=PAID` 인 경우 마지막 입금일 |
| **잔여일** | `예정일 - 오늘`. 음수=남은일수(초록), 0=오늘(노랑), 양수=연체일수(빨강) |
| **상태** | `🟢 미도래 / 🟡 오늘 / 🟠 임박(±3일) / 🔴 연체 / ✅ 완납` |

### 요약 (표 하단)
- **미결제 합계**: 미결제 건수와 금액
- **연체**: 연체 건수와 금액 (있으면 빨강)

## 7.3 데이터가 없을 때

이용 내역이 없으면 안내 메시지 + **[견적 요청하기]** CTA 버튼 — 클릭 시 견적 요청 페이지로 해당 서비스 타입이 자동 채워진 채 이동.

---

# 8. 견적 요청 (NEW)

신규 IT 렌탈, 계측기 렌탈, 교정, 수리, 매매, 유지보수 견적을 즉시 요청합니다.

## 8.1 입력값별 데이터 영향도

| 입력 | DB 필드 | 효과 |
|---|---|---|
| **유형** | `quoteType` (RENTAL_IT/RENTAL_TM/CALIBRATION/REPAIR/PURCHASE/MAINTENANCE/OTHER) | 영업담당 자동 배정의 기준 |
| **제목** (KO/VI/EN 중 1언어 이상) | `titleKo/Vi/En` | 입력 안 한 언어는 Claude API 자동 번역 |
| **상세 설명** | `descriptionKo/Vi/En` | 동일 자동 번역 |
| **희망 수량 / 시작일 / 종료일** | `quantity / desiredStartDate / desiredEndDate` | 영업담당이 견적 작성 시 참고 |

## 8.2 접수 후 흐름

1. **자동 코드 발급** — `QR-YYMMDD-###`
2. **상태 = REQUESTED** → 영업담당 배정 대기
3. Tellustech 영업담당이 견적서 PDF + 금액 작성 → **상태 = QUOTED**
4. 고객 화면에 [수락] / [거절] 버튼 활성화
5. 수락 시 `상태 = ACCEPTED` → 영업담당이 IT계약/TM렌탈/매출로 전환
6. **포인트 적립**: 요청 시점 즉시 +1,000d 자동

## 8.3 내 견적 이력

요청한 모든 견적이 표로 관리됩니다 — 요청일, 코드 (`QR-...`), 유형, 상태, 견적액, 액션 (수락/거절).

---

# 9. 칭찬 / 개선 / 제안 (NEW)

서비스나 직원에 대한 의견을 등록합니다.

## 9.1 종류 선택

| 종류 | 의미 | 포인트 |
|---|---|---|
| 🌟 **칭찬 (PRAISE)** | 직원 개인의 좋은 응대를 칭찬 | +1,000d |
| 🔧 **개선 (IMPROVE)** | 현재 서비스 중 개선이 필요한 점 | +1,000d |
| 💡 **제안 (SUGGEST)** | 새로운 기능·서비스 아이디어 | +1,000d |

## 9.2 작성

- 종류 선택 → 내용 작성 (KO/VI/EN 중 1언어 이상). 자동 번역됨.
- 칭찬 시 **대상 직원** 선택 가능 (본인 거래처의 AS·영업 담당자 목록).
- 등록 후 코드 `FB-YYMMDD-###` 발급.

## 9.3 답변

Tellustech 가 답변을 작성하면 의견 카드 하단에 답변 박스가 추가로 표시됩니다 (3언어).

---

# 10. 소식 (CMS 게시판) (NEW)

마케팅 소식, 회사 공지, 한국·베트남·산업 뉴스, 사용 팁, 고객 커뮤니티 글을 모은 게시판입니다.

## 10.1 카테고리 탭

전체 / MARKETING / COMPANY_NEWS / KOREA_NEWS / VIETNAM_NEWS / INDUSTRY_NEWS / TIP / COMMUNITY

## 10.2 게시글 보기

- 카드 클릭 → 본문이 카드 아래에 펼쳐집니다.
- **출처 URL** 이 본문에 포함된 경우 자동으로 클릭 가능한 링크로 표시.
- **AI 자동 생성 초안** 안내가 본문 끝에 있을 수 있음 — 그 경우는 사실 검증 후 수정될 수 있습니다.
- **상단 고정 (📌)** 표시된 글은 항상 맨 위.

## 10.3 보너스 포인트

- 일부 게시글에는 `+N d` 뱃지가 붙어 있습니다 (관리자가 설정).
- 처음 열람 시 1회만 자동 적립 — 이후 재열람은 적립 없음.
- 적립 즉시 사이드바 🏆 잔액 갱신.

## 10.4 커뮤니티 글 작성 (선택)

`COMMUNITY` 카테고리는 고객도 글을 작성할 수 있습니다 (Phase C 운영 시점에 활성화). 작성 즉시 +1,000d 적립.

---

# 11. 서베이 (NEW)

고객 만족도 조사 등 Tellustech 가 발행한 설문에 응답합니다.

## 11.1 진행 중 서베이 카드

각 서베이 카드:
- 제목, 진행 기간 (시작 ~ 종료)
- **🏆 +10,000d** 보상 표시
- **[참여하기]** 버튼 (또는 이미 참여 시 「참여 완료」)

## 11.2 참여 모달

질문 유형:
- **RATING** — 1~5 점수 선택
- **CHOICE** — 보기 중 단일 선택
- **TEXT** — 자유서술 (자동 번역됨)

[참여하기] 제출 시:
1. 서버 검증 → 본인 거래처 + 진행 기간 내 + 미참여
2. 답변 저장 (`SurveyResponse.answers` JSON)
3. **+10,000d 자동 적립** (서베이마다 단가 다를 수 있음)
4. 토스트 알림 + 사이드바 잔액 갱신

> **한 거래처 = 1회 응답** 만 허용됩니다.

---

# 12. 업체 추천 (NEW)

알고 있는 다른 업체를 Tellustech 에 소개합니다. 첫 입금이 발생하면 추천인에게 **100,000d** 가 적립됩니다.

## 12.1 입력 항목

| 항목 | 필수 |
|---|---|
| 업체명 | ✓ |
| 담당자명 | ✓ |
| 전화번호 | ✓ |
| 이메일 | (선택) |
| 추천 사유 | (선택) |

> **자기 회사 추천 차단** — 본인 거래처와 같은 회사명은 등록 시 거부됩니다 (`self_referral` 에러).

## 12.2 진행 단계

| 단계 | DB 값 | 의미 | 포인트 |
|---|---|---|---|
| 제출 | SUBMITTED | 등록 완료 | — |
| 연락 완료 | CONTACTED | 영업담당이 1차 연락 | — |
| 미팅 진행 | MEETING | 영업미팅 진행 중 | — |
| 계약 체결 | CONTRACTED | 정식 계약 (아직 입금 전) | — |
| **첫 입금** | PAID | 신규 거래처 첫 송금 확인 | **+100,000d 자동 적립** |
| 거절 / 무산 | DECLINED | 진행 중단 | — |

> **계약 시점이 아닌 첫 입금 시점에 적립** — 비현금 계약이나 취소 리스크를 차단하기 위한 설계입니다.

## 12.3 내 추천 이력

요청한 모든 추천이 표로 관리됩니다.

---

# 13. 내 포인트 (NEW)

`/portal/points` 또는 사이드바 🏆 클릭으로 진입.

## 13.1 잔액 + 진행바

```
🏆 87,000d
[████████░░░░░░░░░░░░░░░░] 8.7%
1,000,000d 까지 913,000d 남음
[포인트 교환] (1,000,000d 부터 활성화)
```

## 13.2 적립 / 차감 이력

날짜 · 사유 · 적립/차감 · (각 행이 누적되어 잔액 = SUM 으로 항상 정확).

## 13.3 적립 가이드 (요약)

| 활동 | 단가 |
|---|---|
| AS 요청 / 소모품 요청 / 서비스 확정 / 사용량 컨펌 | 1,000d |
| 견적 요청 / 칭찬·개선·제안 | 1,000d |
| 커뮤니티 글 작성 | 1,000d |
| 보너스 게시글 열람 (1회) | 글마다 다름 |
| 서베이 응답 | 10,000d |
| 추천 업체 첫 입금 | 100,000d |

> 적립 단가는 Tellustech 관리자가 변경할 수 있습니다 (정책 변경 시 즉시 반영).

## 13.4 포인트 교환 (1,000,000d 부터)

[포인트 교환] 버튼 클릭 → 모달:

### 방법 1: 💰 청구액 차감
다음 IT 계약 / 매출 청구액에서 자동 차감. 영업일 3일 내 처리.

### 방법 2: 🎫 상품권 수령
기프티콘 발송 (담당자가 등록된 이메일·휴대폰으로 송부).

### 단위
- 최소 **1,000,000d**
- **1,000,000d 단위** (200만, 300만 등)
- 잔액 한도 내

### 처리 흐름
1. 신청 → `PortalReward(status=PENDING)` 생성, 즉시 잔액 차감
2. Tellustech 관리자 승인 → `APPROVED` (실제 처리 진행)
3. 처리 완료 → `DELIVERED` (영업일 3일 내)
4. 거절 시 `REJECTED` + 차감된 포인트 자동 환원

> **중복 차감 방지** — 한 신청이 처리 중일 때 같은 금액 신청을 중복 등록할 수 있으나 잔액에서 모두 차감됩니다.

---

# 14. 모바일 PWA 설치

포탈은 PWA(Progressive Web App) 로 동작하므로 스마트폰 홈 화면에 앱처럼 설치할 수 있습니다.

## 7.1 iOS Safari

1. Safari 로 포탈 주소에 접속해 로그인까지 완료합니다.
2. 하단 가운데 **공유** 버튼(□↑) 을 누릅니다.
3. **「홈 화면에 추가」** 를 선택합니다.
4. 이름을 확인하고 **추가** 를 누르면 홈 화면에 아이콘이 생성됩니다.

## 7.2 Android Chrome

1. Chrome 으로 포탈 주소에 접속해 로그인까지 완료합니다.
2. 우상단 **⋮ 메뉴 → 「홈 화면에 추가」** 를 선택합니다.
3. **설치** 를 누르면 앱 서랍·홈에 아이콘이 생성됩니다.

## 7.3 앱처럼 사용하기

- 설치 후에는 브라우저 주소창 없이 전체 화면으로 실행됩니다.
- 일부 화면(메인·내 요청 현황 등)은 오프라인에서도 마지막 본 데이터를 캐시로 표시합니다. 새 요청·실시간 상태 갱신은 인터넷 연결이 필요합니다.

---

# 15. FAQ

**Q. 비밀번호를 잊었습니다.**
A. Tellustech 담당자에게 연락해 재설정을 요청해 주세요. 자가 재설정은 제공하지 않습니다.

**Q. AS 요청 진행 상황은 어디서 봅니까?**
A. 메인 화면 「내 요청 현황」 표에서 4단계(요청 → 진행중 → 완료 → 확인됨)로 추적됩니다. 알림은 등록된 이메일·카카오로 함께 전송됩니다.

**Q. 소모품 목록에 원하는 품목이 보이지 않습니다.**
A. 호환 매핑이 등록되지 않았을 가능성이 큽니다. 담당자에게 해당 품목을 본인 장비에 매핑해 달라고 요청해 주세요.

**Q. 사용량 컨펌에서 카운터 값이 잘못된 것 같습니다.**
A. **컨펌 버튼을 누르기 전에** 담당자에게 연락해 주세요. 컨펌이 완료되면 수정할 수 없습니다.

**Q. 회사 헤더에 빨간 정지 배지가 떠 있습니다.**
A. 미수금 결제가 지연되어 신규 요청 메뉴가 일시 비활성화된 상태입니다. 결제 확인 후 자동 해제됩니다. 사용량 컨펌·교정 성적서 다운로드는 정지와 무관하게 사용 가능합니다.

**Q. 알림은 어디로 옵니까?**
A. 등록된 이메일 / 카카오톡으로 전송됩니다. 수신 채널 변경은 담당자에게 문의해 주세요.

**Q. 한국어와 베트남어를 동시에 보고 싶습니다.**
A. 입력 시에는 원문 언어 한 개만 선택해 작성하면 시스템이 자동으로 나머지 언어를 번역해 보관합니다. 표시 언어는 화면 우상단 국기 아이콘으로 전환할 수 있습니다.

**Q. 사이드바가 갑자기 사라졌습니다 / 모바일에서 메뉴를 못 찾겠습니다.**
A. PC: 사이드바 우상단 ◀ 버튼을 누르면 64px 로 접힙니다. ▶ 클릭으로 다시 펼치세요. 모바일: 화면 좌상단 ☰ 햄버거 메뉴를 누르면 좌측에서 메뉴가 나옵니다.

**Q. 가로화면(landscape)으로 사용하고 싶은데 자동 회전이 안 됩니다.**
A. PWA 로 설치된 경우 manifest 가 갱신될 때까지 약간 시간이 걸립니다. 홈 화면의 아이콘을 한 번 삭제하고 Safari/Chrome 에서 재접속 후 다시 「홈 화면에 추가」 하시면 됩니다. 일반 브라우저로 접속한 경우는 페이지 새로고침으로 즉시 적용됩니다.

**Q. 포탈 로그인 후 사내 ERP(직원용) 로 들어가고 싶습니다.**
A. 사이드바 하단(또는 모바일 햄버거 메뉴 하단)의 **🏢 사내 직원 로그인** 버튼을 누르세요. 자동 로그아웃 후 사내 로그인 화면으로 이동합니다.

**Q. 포인트는 언제 적립되나요?**
A. AS·소모품·서비스 확정·사용량 컨펌 → 즉시 +1,000d. 견적 요청·의견 등록 → 즉시 +1,000d. 서베이 → 응답 즉시 +10,000d. 추천 → 신규 거래처가 첫 입금 시 +100,000d (계약 시점 아님).

**Q. 포인트는 언제 사용 가능합니까?**
A. 누적 1,000,000d 부터 교환 가능. `/portal/points` 또는 사이드바 🏆 클릭 → [포인트 교환]. 청구액 차감 또는 상품권 수령 중 선택. 신청 후 영업일 3일 내 처리.

**Q. 견적 요청 후 며칠 안에 답변이 오나요?**
A. Tellustech 영업담당이 자동 배정되어 통상 1~2 영업일 내 답변. 긴급 시 담당자에게 직접 전화 연락 권장.

**Q. 추천한 업체가 계약했는데 포인트가 안 들어왔습니다.**
A. 100,000d 적립은 **첫 입금 발생 시점**에 처리됩니다. 계약 체결만으로는 적립되지 않습니다. 입금 확인 후에도 누락된 경우 담당자에게 문의해 주세요.

**Q. 게시글에 출처 URL 이 있는데 클릭이 안 됩니다.**
A. 포탈에서는 자동으로 클릭 가능한 링크로 변환됩니다. 만약 평문으로 보이면 페이지 새로고침해 주세요.

**Q. 견적 요청 / 의견 등록 기능 메뉴를 눌렀는데 「준비 중」 페이지가 떠요.**
A. 일부 기능은 단계적으로 활성화되고 있습니다. 곧 사용 가능하니 조금만 기다려 주세요.

---

## 연락처 (Contact)

본 매뉴얼에 다루지 않은 문의나 긴급 AS 는 아래 연락처로 부탁드립니다.

- **Tellustech Vietnam** — 전화 / 이메일 (담당자 명함 참조)
- **Vietrental** — 전화 / 이메일 (담당자 명함 참조)
- **긴급 AS 핫라인** — (담당자 명함 참조)

> 정확한 연락처는 담당자 명함 또는 계약서 첫 장을 참조해 주세요. 문서가 의심스러운 경로로 전달된 경우 반드시 직접 담당자에게 확인 후 사용해 주세요.
